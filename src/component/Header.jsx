import React from "react";
import ReactDom from "react-dom";

export default function Header() {
    const title = "Keeper";
    return (
        <header>
            <h1>{title}</h1>
        </header>
    );
}
